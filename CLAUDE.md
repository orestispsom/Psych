# Claude Code instructions

Read and follow `AGENTS.md` before acting. It contains the repository-wide execution rules, including the reusable-knowledge detection rule.

When a **REUSE CANDIDATE** is surfaced, keep the primary app/exam task first and do not create derivative material unless the user explicitly opts in.
