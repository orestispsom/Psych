// Donation/support prompts have been permanently retired from the app.
// Keep this no-op export temporarily so older App.jsx imports cannot surface
// a prompt while the remaining legacy wiring is removed.
export default function SupportWidget() {
  return null;
}
